/**
 * Supabase client stub for Jest.
  * Exports `supabase = null` so all sync.ts functions short-circuit
   * immediately — no real network calls during unit tests.
    *
     * For integration-style tests that need a mock client, replace with a
      * proper jest.fn() chain that returns controlled data.
       */

       export const supabase = null;
